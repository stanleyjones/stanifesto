{{> header }}

		<article>

			<header>
				<h6><time datetime="{{ date }}">{{ date }}</time></h6>
				<h1>{{{ title }}}</h1>
			</header>

			<section>
				{{{ contents }}}
			</section>

		</article>

{{> footer }}